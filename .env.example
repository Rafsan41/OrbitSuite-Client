# Copy to .env.local and adjust. Never commit .env.local.

# Base URL of the OrbitSuite API, including the version prefix.
# NEXT_PUBLIC_ because the browser makes these calls directly — the value is
# inlined into the bundle at build time, so it must never hold anything secret.
NEXT_PUBLIC_API_URL="http://localhost:5000/api/v1"
